# Auto detect text files and perform LF normalization on checkout.
* text=auto

# Windows batch and PowerShell scripts: force CRLF line endings.
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Unix shell scripts: force LF line endings (executability depends on it).
*.sh text eol=lf
